import { CreateOffice, OfficesList } from 'components/Office';
import { FC } from 'react';

const OrgOffices: FC = () => (
  <>
    <CreateOffice />
    <OfficesList />
  </>
);

export default OrgOffices;
